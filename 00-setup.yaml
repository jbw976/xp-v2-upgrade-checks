# Installs provider-nop, which supplies the NopResource type used by the
# other fixtures' Compositions. Apply before the rest; setup.sh handles
# ordering.
---
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-nop
spec:
  package: xpkg.crossplane.io/crossplane-contrib/provider-nop:v0.4.0
